###### [バージョン 2.1.12]

1.設定画面の着信音の設定で、プリセットの着信音の音声ファイルを指定できるようにしました。
\[使い方\]
 operator-console/sounds/ringtoneフォルダに任意の着信音の音声ファイルを配置します。
 operator-console/sounds/ringtoneフォルダに、filenames.txtファイルを配置します。
  filenames.txtファイルの中身には、配置した着信音の音声ファイル名を改行の区切りで書き込みます。
  (例)
  fileA.wav
  fileB.wav

 以上の設定を行うと、配置した着信音の音声ファイルを、着信音の設定欄でプリセットとして選択できるようになります。

2.オートダイヤルの通話履歴や電話帳の発信ボタンを押した際に、即時に発信するのではなく、ダイヤル入力がされるようにできるようにしました。
　続けて発信ボタンもしくは、任意のラインボタンを押すと発信できます。
 設定は、設定画面の"発信ボタンを押すと即発信する。"のチェックボックスで変更することができます(デフォルトはチェックありです)。

3.エクステンションステータスウィジェットのエクステンション名の表示位置を中央に寄せるようにしました。

4.クイックビジー(発信先候補の表示)の修正
 より適切なときに発信先候補を表示するようにしました。
5.転送ボタンの修正
　通話中の通話を操作していないときは、転送ボタンを押せないようにしました。

6.キャンプオンの修正
 キャンプオン(マニュアル/アテンド転送)のときに、転送元は保留を解除しないと切断ができない(つまり転送ができない)不具合を修正しました。
 キャンプオンでの転送時にも、通話が保留中であっても(転送を完了するために)切断できるようにしました。

7.トグル保留(保留解除)、保留解除ボタンを押したときに、転送中という状態を破棄するようにしました。
 これまでは保留を解除しても(転送を保留解除により中止しても)、転送キャンセルボタンが点滅し続けていました。

8.通話中の通話を操作している場合で(ウィジェットの)発信ボタンが押された際に、"転送（アテンド転送もしくはブラインド転送）する"か、"新規通話で発信する"かのモーダルウィンドウを開くようにしました。
 ショートダイヤルに対応しています。

9.オートダイヤルの電話帳のコンタクト一覧の表示が遅いのを修正しました。
 コンタクト一覧では、コンタクトの詳細情報を取得しないようにしました。
 発信ボタン・削除アイコンなどは、発信できるか削除できるかは試行するまで未確定なので、一度押すまでは仮として表示されます。
 なお、確定された表示については、電話帳のコンタクト一覧が再度表示されたときにまた仮の表示に戻ります。

10.オートダイヤルの通話履歴・内線一覧・電話帳で発信ボタンを押下したときに、ワンタッチ発信が有効でかつ通話中の通話を操作している場合は、
発信方法のモーダルウィンドウを表示するようにしました。

11.ワンタッチダイヤルボタンのウィジェットの設定画面のモードに、"転送もしくは新規発信を選択"を追加しました。
 ワンタッチダイヤルボタンが押下されたときにモードが"転送もしくは新規発信を選択"だった場合に、
通話中の通話を操作している場合は発信方法を選択するモーダルウィンドウが表示され、
そうでなく通話中の通話を操作していない場合には新規の通話として発信します。

12.転送ボタンの設定画面にモードを追加しました。
 モードはアテンド転送か、ブラインド転送かを選択でき、転送のときには選択したモードで転送されます。
なお、デフォルトのモードはアテンド転送になります。

13.ダイヤル入力がされた状態でラインボタンを押下したときに、ラインを使用してダイヤル入力された宛先に発信するようにしました。

14.日本語のアテンデッド転送という言葉を、アテンド転送に変えました。

15.レイアウト編集のタブの表示・編集欄の表示が、タブ名を変更しても無名のタブ(Untitled tab)になっていたのを修正しました。

16.Webphoneのバージョンを2.15.5にアップデートしました。

###### [バージョン 2.1.11]

1.一般ユーザーでログインしたときに、左上のメニューにサインアウトのリンクが正常に表示されない不具合を修正しました。

2.内蔵しているWebphoneを更新しました(更新前のバージョン(2.15.3)とバージョン表記は同じですが、更新日時が変わっていたため更新しました)。

###### [バージョン 2.1.10]

1.レイアウトの削除をできるようにしました。
レイアウトの削除のウィンドウは、ログイン後に右上のメニューから選択することができます。

###### [バージョン 2.1.9]

1.オートダイヤルの直近の履歴の初期の表示順を、"発着信数(降順)"から"開始日時(降順)"に変更しました。

###### [バージョン 2.1.8]

1.オートダイヤルの電話帳で、"共有のコンタクトのみ"のスイッチを動かしたときに、コンタクトの一覧を再表示するようにしました。

2.画面右上のメニューのアイコンのスタイル(表示位置など)を、
カスタムCSSファイル(operator-console_custom.css)を編集することによって変更できるようにしました

.brOC .menuIcon {
  position: absolute;
  top: 4px;
  right: 4px;
  z-index: 15;
}
の中身を変更してください。
(rightの値を大きくすると、メニューアイコンを左に寄せることができます。)

3.画面右上のメニューのアイコンの位置を(右端から20ピクセル)左にずらしました。

4.ノートウィジェットにラベルの項目を追加しました。
ラベルがある場合は、ノートウィジェットのタイトルとして表示することができます。

5.オートダイヤル(オートダイヤルボタンを押下すると表示される)の電話帳のコンタクトを保存したときに、コンタクトの一覧を自動で再度読み込みするようにしました。

6.(Webphoneオプションを使用しているときで)転送をキャンセルしたときに、通話が保留のままになる不具合を修正しました。

7.オートダイヤルの電話帳関連の細部ロジックを修正しました

###### [バージョン 2.1.7]

1.ボタンとテーブルの点滅と点灯をカスタマイズ(CSSファイルによる)できるようにしました。
operator-console/operator-console_custom.css.orgファイルを、
operator-console_custom.cssにリネームし、ファイル内容を編集して使用してください。
変更をしたあとは、ブラウザのキャッシュが効いて変更が反映されない可能性があるので、
operator-console/operator-console.htmlのファイル内容の、
\<link rel="stylesheet" href="./operator-console_custom.css?v=241127"\>
の
v=241127の部分などを適当な文字列などに変更してください。

2.現在使用中のレイアウト名を、画面の右上の設定ボタンを押したときに表示するようにしました。

3.電話帳のコンタクト関連(オートダイヤルビューボタンを押下で表示)の修正を行い、多言語などに対応しました。

4.存在しない電話番号に発信した際に、1度の発信で通話履歴が2つ残らなくなりました。

5.通話履歴が壊れた場合に、レイアウトの画面が開けなくなる問題を修正しました。
その他、細部の修正を行いました。

###### [バージョン 2.1.5]

1.ウィジェットにトグルホールドボタンを追加しました。

2.ウィジェットの転送ボタンで、(転送時には)転送キャンセルをできるようにしました。

3.転送中に通話を保留解除せずに切断できるようにしました。

4.ウィジェットの"前の通話に切り替える"ボタンと"次の通話に切り替える"ボタンを押すことで、新しい通話に切り替えることができるようになりました。

5.オートダイヤルの電話帳の情報アイコンをクリックした際に、エラーが表示されないようにしました。

6.レイアウトを編集しているときに、選択したウィジェットを囲う線を表示するようにしました。

7.ログイン画面の右下に、OpeartorConsoleのバージョンを表示するようにしました。

8.ログイン画面にユーザーの言語でテキストが表示されるようにしました。

その他、細部の修正を行いました。