###### [バージョン 2.1.26]
1. オートダイヤルビューの電話帳(英語:Phonebook)のタブの電話番号編集ビューが広くなった時に、ブラウザのウィンドウからはみ出してしまう不具合を修正しました。<br />
はみ出してしまいそうになる場合には、はみ出さずにスクロールバーが表示されます。

1. オートダイヤルビューの左側の余白にある、レイアウトのボタンなどが操作できない不具合を修正しました。

1. ボタンウィジェットの設定ビューのツールチップの入力欄をテキストエリアにし、複数行を入力できるようにしました。これによりツールチップに改行が使用できます。

1. オートダイヤルビューの直近の履歴(英語:Recent)のタブにある年月日・日時分の範囲の取得条件を、開始日時分以降から終了日時分前にしました。

1. オートダイヤルビューを初めて開いたときに、直近の履歴(英語:Recent)のタブの表を自動で表示するようにしました。当日の年月日の履歴が表に表示されます。

1. オートダイヤルビューの直近の履歴(英語:Recent)のタブの表を、通話の状況が更新されたときに自動で再表示するようにしました。

1. オートダイヤルビューを表示したままで設定(英語:Settings)画面を開き保存したときに、オートダイヤルビューのボタンが点灯しない状態になる不具合を修正しました。

1. オートダイヤルビューの各表のレイアウトを調整しました。
###### [バージョン 2.1.25]
1. ボタンウィジェットにツールチップをボタンウィジェットの設定ビューで設定できるようにしました。ツールチップはボタンウィジェットにマウスカーソルを合わせると表示されます。<br />
ツールチップを設定していない場合は初期のツールチップが表示されます。なお、ボタンが不活性の場合はHTMLの仕様により表示されません。

1. オートダイヤルビューのタブを切り替えたときに、オートダイヤルビューが右にはみ出てしまう不具合を修正しました。

1. オートダイヤルビューの直近の履歴(英語:Recent)のタブにある年月日・日時分の範囲のフィルタに、実在しない年月日を選択できないように変更しました。

1. オートダイヤルビューの直近の履歴(英語:Recent)のタブにある年月日・日時分の範囲のフィルタの時・分を、1分単位から15分単位で選択できるように変更しました。

1. オートダイヤルビューの直近の履歴(英語:Recent)のタブの表の<br />
開始日時(英語:STARTED AT)・応答日時(ANSWERED AT)・終了日時(ENDED AT)の項目値の表示位置を、左寄せから中央寄せに変更しました。

1. エスケープキーを押した時に、コールパネルにEscapeと表示されていたのを修正しました。
###### [バージョン 2.1.24]
1. オートダイヤルビューの直近の履歴(英語:Recent)のタブの表のフィルタで、実在しない年月日を入力して検索ボタンを押すとエラーになる不具合を修正しました。

1. オートダイヤルビューのタブを直近の履歴(英語:Recent)に切り替えたときに、オートダイヤルビューの位置が左寄りに表示される不具合を修正しました。

1. オートダイヤルビューのタブを切り替えたきの、オートダイヤルビューの表示の一瞬の乱れ(表の幅や高さの変化)を軽減しました。<br />
表示内容を表の中に非同期で挿入しているため、いくらかは乱れが残っています。

1. オートダイヤルビューの直近の履歴(英語:Recent)の表の上にある"詳細を表示(英語:Show detail)"のチェックボックスを切り替えたときの、オートダイヤルビューの表示の一瞬の乱れ(表の幅や高さの変化)を軽減しました。

###### [バージョン 2.1.23]
1. 長時間使用せずにログアウトした際に、通話履歴の保存に失敗する不具合を修正しました。<br />
なお、通話の終了や発着信などと同時にブラウザやブラウザのタブを閉じる際にはしばらく待ってからでないと、それらの通話の通話履歴は保存されません。<br />
(ブラウザやブラウザのタブを閉じずにログアウトした際には、通話履歴の保存がすぐ実行開始されます。)

1. オートダイヤルビューの直近の履歴(英語:Recent)のタブの表の上に、<br />
(開始日時(英語:STARTED AT)に該当する)履歴の表示対象を、開始年月日と終了年月日で範囲指定するためのフィルタを配置しました。<br />
フィルタは設定(英語:Settings)の画面で、"履歴の表示順(英語:History display order)"が"開始日時(降順)(英語:Started at(desc))"になっているときだけ指定できます。<br />
なお、フィルタはデフォルト(初期状態)で本日の0時0分~23時59分がセットされます。

1. "開始時間(英語:Answered at)"の日本語を、"応答日時"に変更しました。

1. 表の(最上段の)見出しを、左寄せから中央寄せに変更しました。
###### [バージョン 2.1.22]
1. オートダイヤルビューで詳細を表示(英語:Show detail)のチェックボックスのチェックを付けたときに、オートダイヤルビューがブラウザのウィンドウの右にはみ出るのを修正しました。

1. オートダイヤルビューのユーザーのタブの表の上に、表の表示対象を絞り込むためのフィルタを配置しました。
###### [バージョン 2.1.21]
1. オートダイヤルビューで詳細を表示(英語:Show detail)のチェックボックスのチェックを外したときに、オートダイヤルビューの位置が右寄せでなくなるのを修正しました。

1. オートダイヤルビューがブラウザのウィンドウをはみ出てしまう場合は、スクロールバーを出してブラウザのウィンドウをはみ出てしまわないように修正しました。

1. オートダイヤルビューの電話帳のコンタクト情報ビューでブラウザのウィンドウをはみ出てしまう場合は、スクロールバーを出してブラウザのウィンドウをはみ出てしまわないように修正しました。

1. オートダイヤルビューで電話帳の電話情報ビューでブラウザのウィンドウをはみ出てしまう場合は、スクロールバーを出してブラウザのウィンドウをはみ出てしまわないように修正しました。

1. オートダイヤルビューの電話帳のコンタクト情報ビューでコンタクトを削除した際に、コンタクト一覧ビューを自動で再読込・再表示するようにしました。
###### [バージョン 2.1.20]
1. オートダイヤルビューで初めにユーザー(英語:User)のタブをクリック(タッチ)した際に、オートダイヤルビューのユーザーの表の表示が乱れるのを修正しました。
###### [バージョン 2.1.19]
1. オートダイヤルビューの直近の履歴(英語:Recent)タブの詳細を表示(英語:Show detail)のチェックボックスをクリック(タッチ)したあとに、オートダイヤルビューの表の表示が乱れるのを修正しました。
###### [バージョン 2.1.18]
1. 他のユーザーで素早くログインし直した際に、ログイン前のユーザーの通話履歴が残るおそれがあることへの修正を行いました。
###### [バージョン 2.1.17]
1. オートダイヤルビューの電話帳(のコンタクト一覧)のコンタクト情報に、電話帳の共有を示す"共有"の列を追加しました。

1. "会議"ボタンを押して成功したときに、転送キャンセルボタンの点滅を消すようにしました。

1. (オートダイヤル欄などからでの)通話中の通話を転送する前に通話が保留でない場合は、通話中の通話を保留にしてから転送をするようにしました。

1. オートダイヤルビューの電話帳のコンタクトを保存する際に、コンタクト情報の隠し項目に対して、項目名を変更して保存していた不具合を修正しました。

1. 通話が通話中か保留中でない場合は、転送を試みても転送をしないようにしました。

1. エディタビューのタブ欄をクリック(タッチ)状態にし続けたときに、プロパティ欄(右欄)のメニューがタブの"有効・無効切り替え/画面分割"の内容に切り替わらないようにしました。

1. ログアウトした直後に、「通話履歴の保存に失敗しました。」という旨のメッセージが表示されないようにしました。
###### [バージョン 2.1.16]
1. "削除する"ボタンなどの枠線が消えていた不具合を修正しました。

1. 外側のスペースが無いボタンなどに対して、スペースを加えました。

1. 三者(英語:Three-way)ボタンの名前を会議(英語:Conference)に変更しました。

1. クイックコールボタンがアクティブなときに、キーボードの入力でも発信できるように変更しました。

1. オートダイヤルビューの電話帳のコンタクト詳細のカスタム(任意)の項目を削除できるように変更しました。

1. オートダイヤルビューの電話帳のコンタクト詳細のカスタム(任意)の項目が正常に表示されない不具合を修正しました。

1. オートダイヤルビューの電話帳(のコンタクト一覧)のコンタクト情報に、電話帳(名)の列を追加しました。
###### [バージョン 2.1.15]
1. レイアウトのタブを削除するときの確認のメッセージ(日本語表記)を正しく表示するようにしました。

1. レイアウト編集画面のエクステンションステータスのテンプレート(左部)のタイトルが表示しきれていなかった不具合を修正しました。

1. エクステンションテーブルの"ID"の列名を"内線番号(英語表記:Extension)"に変更しました。

1. オートダイヤルビューの"内線番号(英語表記:Extension)"のタブ名を"ユーザー"に変更しました。

1. オートダイヤルビューの内線番号(英語表記:Extension)のタブの"ID"の列名を、"内線番号"に変更しました。
###### [バージョン 2.1.14]
1. 不答(No answer)ボタンにカーソルを乗せたときに表示されるツールチップに、"(この機能は開発中です)"の記述を追加しました。

1. 回線(Line)ボタンの回線欄とコールパーク(Park)ボタンの番号欄が、同じ種類のボタンを開いたときに同じ入力値で表示される不具合を修正しました。

1. 管理権限を持つユーザーに表示される(画面右上の)、ドロップダウンメニューの"レイアウトの表示"の項目を除去しました(レイアウトが意図せず表示されなくなることもなく、意味を成さないため)。

1. 管理権限を持たないユーザーに表示される(画面右上の)、ドロップダウンメニューの"設定"の項目を除去しました(管理検眼を持たないユーザーは設定を変更できないため)。

1. 内蔵しているWebphoneをバージョン2.16.2に更新しました。
###### [バージョン 2.1.13]
1. ログイン画面を始めに表示した際でも、最後にログインしたユーザーの言語でログイン画面のテキストを表示するようにしました。

1. ダイヤル入力をしてEnterキーを押下した際に通話中の通話を操作している場合は、即時に新たな通話として発信せずに、発信方法の選択のモーダルダイヤログを表示するようにしました。

1. キー入力がされた時に、クイックビジー(発信先の候補)を適切に表示するようにしました。

1. モーダルダイアログのボタンを、使用言語が日本語の場合は日本語で表示するようにしました。<br />
(OKボタンは日本語でも"OK"で表示されます。)

1. オートダイヤルの(ボタンを押下されたときに表示される)通話履歴に転送の項目を用意しました。<br />
転送の項目は、転送を開始した通話の通話履歴にチェックが付きます。<br />
なお、転送を中断した場合や転送に失敗した場合にもチェックが付きます。また、転送を受けた側の通話履歴には、転送の項目に転送のチェックは付きません。<br />
転送の項目は設定画面で"履歴の表示順"が"開始日時(降順)"に選択されているときにのみ表示されます。<br />

1. キャンプオンをするときに、アテンド転送かブラインド転送かを選択できるようにしました。

1. ラインテーブルウィジェットの設定画面に、転送方法の選択欄を追加しました。<br />
ラインテーブルの転送ボタンを押下したときに、"転送方法を選択"が選択されている場合は、転送方法を毎回選択します。<br />
"アテンド転送(通話を切り替える)"が選択されているときは、操作している通話が転送する通話に切り替わり、アテンド転送を開始します。<br />
"アテンド転送"が選択されているときは、アテンド転送を開始します。"ブラインド転送"が選択されているときは、ブラインド転送を行います。

1. レイアウト(SIPServer/PBXのノート)を保存するときのノートの権限を、"読み書き可"から"読み込み可"に変更しました。

1. ビルドして出力されるoperator-console.jsファイルを、画面ごとのjsファイルに分割しました(ログイン画面の表示の時間が、やや短縮されます)。

1. (管理者権限を持つユーザーで)開くレイアウトを選択するときに、存在しないレイアウトが表示される不具合を修正しました。

1. 内蔵しているWebphoneをバージョン2.16.1に更新しました。

###### [バージョン 2.1.12]
1. 設定画面の着信音の設定で、プリセットの着信音の音声ファイルを指定できるようにしました。<br />
    ```
    [使い方]
    1.operator-console/sounds/ringtoneフォルダに任意の着信音の音声ファイルを配置します。
    2.operator-console/sounds/ringtoneフォルダに、filenames.txtファイルを配置します。
    
    filenames.txtファイルの中身には、配置した着信音の音声ファイル名を改行の区切りで書き込みます。

    (例)
    fileA.wav
    fileB.wav
    ```
   以上の設定を行うと、配置した着信音の音声ファイルを、着信音の設定欄でプリセットとして選択できるようになります。

1. オートダイヤルの通話履歴や電話帳の発信ボタンを押した際に、即時に発信するのではなく、ダイヤル入力がされるようにできるようにしました。<br />
　続けて発信ボタンもしくは、任意のラインボタンを押すと発信できます。<br />
 設定は、設定画面の"発信ボタンを押すと即発信する。"のチェックボックスで変更することができます(デフォルトはチェックありです)。

1. エクステンションステータスウィジェットのエクステンション名の表示位置を中央に寄せるようにしました。

1. クイックビジー(発信先候補の表示)の修正<br />
 より適切なときに発信先候補を表示するようにしました。
 
1. 転送ボタンの修正<br />
　通話中の通話を操作していないときは、転送ボタンを押せないようにしました。<br />

1. キャンプオンの修正<br />
 キャンプオン(マニュアル/アテンド転送)のときに、転送元は保留を解除しないと切断ができない(つまり転送ができない)不具合を修正しました。<br />
 キャンプオンでの転送時にも、通話が保留中であっても(転送を完了するために)切断できるようにしました。
 
1. トグル保留(保留解除)、保留解除ボタンを押したときに、転送中という状態を破棄するようにしました。<br />
 これまでは保留を解除しても(転送を保留解除により中止しても)、転送キャンセルボタンが点滅し続けていました。<br />
 
1. 通話中の通話を操作している場合で(ウィジェットの)発信ボタンが押された際に、"転送（アテンド転送もしくはブラインド転送）する"か、"新規通話で発信する"かのモーダルウィンドウを開くようにしました。<br />
 ショートダイヤルに対応しています。<br />

1. オートダイヤルの電話帳のコンタクト一覧の表示が遅いのを修正しました。<br />
 コンタクト一覧では、コンタクトの詳細情報を取得しないようにしました。<br />
 発信ボタン・削除アイコンなどは、発信できるか削除できるかは試行するまで未確定なので、一度押すまでは仮として表示されます。<br />
 なお、確定された表示については、電話帳のコンタクト一覧が再度表示されたときにまた仮の表示に戻ります。

1. オートダイヤルの通話履歴・内線一覧・電話帳で発信ボタンを押下したときに、ワンタッチ発信が有効でかつ通話中の通話を操作している場合は、<br />
発信方法のモーダルウィンドウを表示するようにしました。

1. ワンタッチダイヤルボタンのウィジェットの設定画面のモードに、"転送もしくは新規発信を選択"を追加しました。<br />
 ワンタッチダイヤルボタンが押下されたときにモードが"転送もしくは新規発信を選択"だった場合に、<br />
通話中の通話を操作している場合は発信方法を選択するモーダルウィンドウが表示され、<br />
そうではなく通話中の通話を操作していない場合には新規の通話として発信します。

1. 転送ボタンの設定画面にモードを追加しました。<br />
 モードはアテンド転送か、ブラインド転送かを選択でき、転送のときには選択したモードで転送されます。<br />
なお、デフォルトのモードはアテンド転送になります。
1. ダイヤル入力がされた状態でラインボタンを押下したときに、ラインを使用してダイヤル入力された宛先に発信するようにしました。

1. 日本語のアテンデッド転送という言葉を、アテンド転送に変えました。

1. レイアウト編集のタブの表示・編集欄の表示が、タブ名を変更しても無名のタブ(Untitled tab)になっていたのを修正しました。<br />
1. Webphoneのバージョンを2.15.5にアップデートしました。

###### [バージョン 2.1.11]

1. 一般ユーザーでログインしたときに、左上のメニューにサインアウトのリンクが正常に表示されない不具合を修正しました。

1. 内蔵しているWebphoneを更新しました(更新前のバージョン(2.15.3)とバージョン表記は同じですが、更新日時が変わっていたため更新しました)。

###### [バージョン 2.1.10]

1. レイアウトの削除をできるようにしました。
レイアウトの削除のウィンドウは、ログイン後に右上のメニューから選択することができます。

###### [バージョン 2.1.9]

1. オートダイヤルの直近の履歴の初期の表示順を、"発着信数(降順)"から"開始日時(降順)"に変更しました。

###### [バージョン 2.1.8]

1. オートダイヤルの電話帳で、"共有のコンタクトのみ"のスイッチを動かしたときに、コンタクトの一覧を再表示するようにしました。

1. 画面右上のメニューのアイコンのスタイル(表示位置など)を、
カスタムCSSファイル(operator-console_custom.css)を編集することによって変更できるようにしました。
    ```
    .brOC .menuIcon {
     position: absolute;
     top: 4px;
     right: 4px;
     z-index: 15;
    }
    ```
    の中身を変更してください。<br />
    (rightの値を大きくすると、メニューアイコンを左に寄せることができます。)

1. 画面右上のメニューのアイコンの位置を(右端から20ピクセル)左にずらしました。

1. ノートウィジェットにラベルの項目を追加しました。
ラベルがある場合は、ノートウィジェットのタイトルとして表示することができます。

1. オートダイヤル(オートダイヤルボタンを押下すると表示される)の電話帳のコンタクトを保存したときに、コンタクトの一覧を自動で再度読み込みするようにしました。

1. (Webphoneオプションを使用しているときで)転送をキャンセルしたときに、通話が保留のままになる不具合を修正しました。

1. オートダイヤルの電話帳関連の細部ロジックを修正しました

###### [バージョン 2.1.7]

1. ボタンとテーブルの点滅と点灯をカスタマイズ(CSSファイルによる)できるようにしました。<br />
operator-console/operator-console_custom.css.orgファイルを、<br />
operator-console_custom.cssにリネームし、ファイル内容を編集して使用してください。<br />
変更をしたあとは、ブラウザのキャッシュが効いて変更が反映されない可能性があるので、<br />
operator-console/operator-console.htmlのファイル内容の、<br />
\<link rel="stylesheet" href="./operator-console_custom.css?v=241127"\>
の、<br />
v=241127の部分などを適当な文字列などに変更してください。

1. 現在使用中のレイアウト名を、画面の右上の設定ボタンを押したときに表示するようにしました。

1. 電話帳のコンタクト関連(オートダイヤルビューボタンを押下で表示)の修正を行い、多言語などに対応しました。

1. 存在しない電話番号に発信した際に、1度の発信で通話履歴が2つ残らなくなりました。

1. 通話履歴が壊れた場合に、レイアウトの画面が開けなくなる問題を修正しました。

    その他、細部の修正を行いました。

###### [バージョン 2.1.5]

1. ウィジェットにトグルホールドボタンを追加しました。

1. ウィジェットの転送ボタンで、(転送時には)転送キャンセルをできるようにしました。

1. 転送中に通話を保留解除せずに切断できるようにしました。

1. ウィジェットの"前の通話に切り替える"ボタンと"次の通話に切り替える"ボタンを押すことで、新しい通話に切り替えることができるようになりました。

1. オートダイヤルの電話帳の情報アイコンをクリックした際に、エラーが表示されないようにしました。

1. レイアウトを編集しているときに、選択したウィジェットを囲う線を表示するようにしました。

1. ログイン画面の右下に、OpeartorConsoleのバージョンを表示するようにしました。

1. ログイン画面にユーザーの言語でテキストが表示されるようにしました。

   その他、細部の修正を行いました。